/**
 * @file: Title
 * @author: AUTHOR
 * @description: 
 */

import A from 'assert';

import {deepEqual, deepStrictEqual} from 'assert';
import * as hhh from 'assert';

import unuse from 'domain';
let unuse2 = require('dns')
let {unuseInner} = require('dns')

var hello = {x: '22'}//'world'
hello.xxxx = {}
hello.xxxx.a = '222'
exports.h = 'woll';
export default {
    a() {
        const unuse3 = require('stream')
        let x = unuse2;

        setTimeout(() => {
            unuse2 = 123;
            let unuse4 = require('events')
        })
        return A.deepEqual;
    }
}