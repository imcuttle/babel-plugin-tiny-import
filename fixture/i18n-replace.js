/**
 * @file: Title
 * @author: AUTHOR
 * @description: 
 */

export {a as A} from 'asd';

var xxx = 's是的';

import {ass, abc} from './wrap'

import A from 'assert';

import unuse from 'domain';
let unuse2 = require('dns');


var x = `sd是的${unuse2}
sdsxxn是的我`

'你和ssd'

"swaabcd"

export default {
    a() {
        const unuse3 = require('stream')

        setTimeout(() => {
            unuse2 = 123;
            let unuse4 = require('events')
        })
        return A.deepEqual;
    }
}